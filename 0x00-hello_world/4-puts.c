#include <stdio.h>
/**
*main : print a string                                                               
*
*return : always (0) for successful run
*/

int main(void)
{
        puts("programming is like building a multilingual puzzle");

        return (0);
}
